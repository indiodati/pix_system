<h1 class="text-3xl font-bold mb-6">Pague seu PIX</h1>

<div class="grid md:grid-cols-2 gap-8 items-start">
  <div class="flex flex-col items-center">
    <h2 class="text-xl font-semibold mb-4">Escaneie o QR Code</h2>

    <% if @pix_qrcode_base64.present? %>
      <div class="border rounded-lg p-4 bg-white shadow">
        <img src="data:image/png;base64,<%= @pix_qrcode_base64 %>" >
        
      </div>
    <% else %>
      <p class="text-red-500">Não foi possível carregar o QR Code.</p>
    <% end %>
  </div>

  <div>
    <h2 class="text-xl font-semibold mb-4">Ou copie e cole o código PIX</h2>

    <% if @pix_copy_paste.present? %>
      <textarea
        readonly
        class="w-full h-40 border rounded px-3 py-2 text-sm bg-gray-50"
      ><%= @pix_copy_paste %></textarea>

      <p class="text-gray-600 mt-2 text-sm">
        Copie esse código e cole no seu app do banco na opção <strong>PIX copia e cola</strong>.
      </p>
    <% else %>
      <p class="text-red-500">Não foi possível carregar o código copia-e-cola.</p>
    <% end %>

    <div class="mt-6">
      <%= link_to "Voltar para transações", pix_transactions_path, class: "text-blue-500 hover:underline" %>
    </div>
  </div>
</div>
