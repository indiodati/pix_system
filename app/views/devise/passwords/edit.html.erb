<h1 class="text-3xl font-bold mb-6">Redefinir Senha</h1>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
  <%= f.hidden_field :reset_password_token %>

  <div class="mb-4">
    <%= f.label :password, class: "block font-medium" %>
    <%= f.password_field :password, autocomplete: "new-password", class: "w-full border px-3 py-2 rounded" %>
  </div>

  <div class="mb-4">
    <%= f.label :password_confirmation, class: "block font-medium" %>
    <%= f.password_field :password_confirmation, class: "w-full border px-3 py-2 rounded" %>
  </div>

  <div class="mb-4">
    <%= f.submit "Redefinir senha", class: "bg-purple-500 hover:bg-purple-700 text-white font-bold py-2 px-4 rounded" %>
  </div>

  <%= link_to "Voltar ao login", new_session_path(resource_name), cl
