<h1 class="text-3xl font-bold mb-6">Recuperar Senha</h1>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <div class="mb-4">
    <%= f.label :email, class: "block font-medium" %>
    <%= f.email_field :email, autofocus: true, class: "w-full border px-3 py-2 rounded" %>
  </div>

  <div class="mb-4">
    <%= f.submit "Enviar instruções", class: "bg-purple-500 hover:bg-purple-700 text-white font-bold py-2 px-4 rounded" %>
  </div>

  <%= link_to "Voltar ao login", new_session_path(resource_name), class: "text-blue-500 hover:underline" %>
<% end %>
